/**
 * cylinders
 *
 * registers the module in the central database
 */
/**
 * @returns {cylinders}
 */
new splash() ;
function	splash() {
	dBegin( 1, "splash.js", "splash", "main()") ;
	wapScreen.call( this, "splash") ;
	dEnd( 1, "splash.js", "splash", "main()") ;
}
